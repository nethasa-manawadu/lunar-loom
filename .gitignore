# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
.env                # 🔥 Prevents uploading secrets (MUST HAVE!)
.env.local          # Local overrides
.env.development    # Dev env files
.env.production     # Prod env files

# Dependencies
node_modules/       # 🚨 Never push this (huge + Vercel installs it)

# Build folders
dist/               # React build output (auto-generated)
dist-ssr/           # SSR build (if using Next.js)
.build/             # Some tools generate this

# IDE/Editor junk
.vscode/            # VS Code settings
.idea/              # JetBrains IDE (WebStorm, etc.)
.DS_Store           # macOS trash
*.suo               # Windows IDE junk
*.ntvs*             # More Windows junk
*.njsproj           # More Windows junk
*.sln               # More Windows junk
*.sw?               # Swap files (Linux/Unix)

# Firebase cache
.firebase/          # Firebase CLI cache
.firebaserc         # Firebase config (if you use aliases)

# Misc
*.local             # Local dev files
.cache/             # Parcel, Gatsby, etc. cache
.temp/              # Temp files